<?xml version="1.0" encoding="UTF-8"?>
<response>
    <result>
        <list>
        {% for location in results %}
            <item>
                <id>{{location.woeid}}</id>
                <city>{{location.name}}</city>
                <countryname>{{location.country_name}}</countryname>
            </item>
        {% endfor %}
        </list>
    </result>
</response>
